# Railway backend deploy — use repo ROOT as service root (not /backend folder)
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node backend/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
